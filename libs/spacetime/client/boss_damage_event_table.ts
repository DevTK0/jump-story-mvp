// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.2.0 (commit fb41e50eb73573b70eea532aeb6158eaac06fae0).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { BossDamageEvent } from "./boss_damage_event_type";
import { AttackType as __AttackType } from "./attack_type_type";
import { DamageType as __DamageType } from "./damage_type_type";

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `BossDamageEvent`.
 *
 * Obtain a handle from the [`bossDamageEvent`] property on [`RemoteTables`],
 * like `ctx.db.bossDamageEvent`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.bossDamageEvent.on_insert(...)`.
 */
export class BossDamageEventTableHandle {
  tableCache: TableCache<BossDamageEvent>;

  constructor(tableCache: TableCache<BossDamageEvent>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<BossDamageEvent> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `damageEventId` unique index on the table `BossDamageEvent`,
   * which allows point queries on the field of the same name
   * via the [`BossDamageEventDamageEventIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.bossDamageEvent.damageEventId().find(...)`.
   *
   * Get a handle on the `damageEventId` unique index on the table `BossDamageEvent`.
   */
  damageEventId = {
    // Find the subscribed row whose `damageEventId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): BossDamageEvent | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.damageEventId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: BossDamageEvent) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: BossDamageEvent) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: BossDamageEvent) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: BossDamageEvent) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: BossDamageEvent, newRow: BossDamageEvent) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: BossDamageEvent, newRow: BossDamageEvent) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
